# GitHub Project Maintenance

This folder contains repository automation used to keep the project reproducible and maintainable.

- `workflows/quality.yml` runs Python compilation and the test suite on pushes and pull requests to `main`.
